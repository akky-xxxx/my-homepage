import { EmptyFunction } from "../../../../../../common";
import { IconCloseProps } from "../../types";
declare type UseIconCloseReturn = {
    size: number;
    handleClick: EmptyFunction;
};
declare type UseIconClose = (args: IconCloseProps) => UseIconCloseReturn;
export declare const useIconClose: UseIconClose;
export {};
