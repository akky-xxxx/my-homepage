// import node_modules
import styled from "styled-components"

// import others
import { Margin } from "../../../../../../const"

// main
const { MARGIN10 } = Margin
export const ModalBody = styled.div`
  flex-grow: 1;
  overflow: auto;
  padding: 0 ${MARGIN10}px;
`
