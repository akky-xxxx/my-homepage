// import node_modules
import styled from "styled-components"
import { Font } from "shared-items/dist/client"

// main
const {
  SIZE: { LARGE },
} = Font
export const Heading2 = styled.h2`
  font-size: ${LARGE};
  font-weight: bold;
`
